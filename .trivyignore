# Trivy vulnerability suppressions for PDI HolmesGPT
#
# Format: CVE-ID [expiry-date] [# comment]
# Expiry dates are optional but recommended to force periodic review.
#
# ── Accepted risks ────────────────────────────────────────────────────────────

# CVE-2026-25990: pillow 10.4.0 out-of-bounds write in PSD image loading.
# Fix requires pillow>=12.1.1, but prometrix==0.2.9 pins pillow<11.0.0.
# Upstream issue — no PDI fix available until prometrix releases a new version.
CVE-2026-25990
